{
    "version": 2,
    "builds": [
      {
        "src": "client/vite.config.js",
        "use": "@vercel/builders",  
        "config": {
          "framework": "vite",
          "build": {
            "outDir": "client/dist"
          }
        }
      },
      {
        "src": "server/server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",  
        "dest": "server/server.js"  
      },
      {
        "handle": "filesystem"
      }
    ]
  }